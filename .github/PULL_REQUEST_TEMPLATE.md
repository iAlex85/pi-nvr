## What does this PR do?

## Why

## Performance impact
Does this change affect CPU/RAM usage on a Pi 3 (new decoding, new
background loop, new dependency)? If so, describe the impact.

## Testing
- [ ] `pytest` passes locally
- [ ] Manually tested against a real or simulated camera (describe how)

## Checklist
- [ ] New tunables were added to `config/default_config.yaml`, not hardcoded
- [ ] `CHANGELOG.md` updated under "Unreleased"
- [ ] Docs updated if user-facing behavior changed
